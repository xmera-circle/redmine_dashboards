$('#errorExplanation').remove();

<% @updated_blocks.each do |block| %>
  $('#content').prepend('<%= escape_javascript(error_messages_for(@dashboard)) %>');
  $("#block-<%= block %>").replaceWith("<%= escape_javascript render_dashboard_block(block.to_s, @dashboard) %>");
<% end %>


$('[title]').tooltip({
    show: {
      delay: 400
    },
    position: {
      my: "center bottom-5",
      at: "center top"
    }
});
